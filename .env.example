# Copy to .env and replace every value. Never commit .env.
POSTGRES_PASSWORD=change-me-postgres
PAYROLL_ENV=development
PAYROLL_SECRET_KEY=change-me-generate-with-python-secrets-token-urlsafe-48
PAYROLL_DEMO_PASSWORD=change-me-demo-users
PAYROLL_LOG_LEVEL=INFO
# Local runs without Docker default to SQLite:
# PAYROLL_DATABASE_URL=sqlite:///./payroll.db
